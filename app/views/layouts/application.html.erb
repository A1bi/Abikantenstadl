<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Abikantenstadl<% if content_for?(:title) %> - <%= yield(:title) %><% end %></title>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag "application" %>
</head>
<body>
  <header class="row">
    <div class="large-4 small-12 columns title"><%= link_to "Abikantenstadl", root_path %></div>
    <% if @_user.id %>
      <div class="large-3 large-offset-5 small-12 columns"><%= @_user.full_name %> - <%= link_to "logout", logout_path %></div>
    <% end %>
  </header>
  <div class="row">
    <div class="small-12 columns">
      <% flash.each do |type, msg| %>
        <div class="alert-box <%= type %>" data-alert>
          <%= msg %>
          <a href="#" class="close">&times;</a>
        </div>
      <% end %>
    </div>
  </div>
  <%= yield %>
  <%= javascript_include_tag "application" %>
	<% if Rails.env.production? %>
		<% as_site = 12 %>
		<% base_url = "http" + ((request.ssl?) ? "s" : "") + "://system.albisigns.de" + ((request.ssl?) ? ":442" : "") + "/" %>
		<script>var as_site = <%= as_site %>;</script>
		<%= javascript_include_tag base_url + "stats.js" %>
		<noscript><%= image_tag base_url + "stats" + as_site.to_s + ".png", :alt => "" %></noscript>
	<% end %>
</body>
</html>
