<%= div_for stories, class: :panel do |story| %>
  <div class="row">
    <div class="large-6 columns">
      <b><%= story.title %></b>
    </div>
    <div class="large-4 columns text-right">
      <em><%= story.document.original_filename %></em>
    </div>
    <div class="large-1 columns">
      <%= render "action_btns", path: edit_story_path(story), del_path: story_path(story), confirm: "diesen Kursbericht" %>
    </div>
  </div>
  <hr />
  <h6>Fotos zu diesem Kursbericht</h6>
  <div class="row">
    <div class="small-12 columns">
      <%= render "photo_gallery", photos: story.photos %>
      <% if story.photos.empty? %>
        <div class="row">
          <div class="small-12 columns">
            <p>Sollte dein Kursbericht Fotos enthalten, lade diese bitte hier noch einmal gesondert hoch.</p>
          </div>
        </div>
      <% end %>
      <%= form_for Photo.new do |f| %>
        <%= f.hidden_field :assignable_type, value: story.class.name %>
        <%= f.hidden_field :assignable_id, value: story.id %>
        <div class="row">
          <div class="large-12 columns">
            <div class="row">
              <div class="large-4 columns">
                <label>Foto hinzufügen</label>
                <%= f.file_field :image, accept: "image/jpg, image/jpeg, image/png" %>
              </div>
              <div class="large-8 columns">
                <%= f.submit "hochladen", class: [:button, :tiny] %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
<% if stories.empty? %>
  <em>Noch keine Kursberichte hochgeladen.</em>
<% end %>