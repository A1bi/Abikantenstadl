<% if errors.any? %>
  <div class="row">
    <div class="small-12 columns">
      <ul class="alert-box alert">
        <% errors.each do |attr, msg| %>
          <%= content_tag :li, msg %>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>